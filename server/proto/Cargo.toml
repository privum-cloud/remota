[package]
name = "remota-proto"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "Shared control-channel messages between remota-agent and remota-relay"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
